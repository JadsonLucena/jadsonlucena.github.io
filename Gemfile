source "https://rubygems.org"

# The site uses a custom Bundler-based Pages build, so it is not constrained to
# the legacy Jekyll version bundled by the github-pages gem.
gem "jekyll", "~> 4.4"
gem "minima", "~> 2.5"
gem "html-proofer", "~> 5.0"
